<UserControl x:Class="Tuvi.App.Shared.Controls.AddressItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:Tuvi.App.Shared.Controls"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">

    <UserControl.Resources>
        <x:Double x:Key="ControlHeigth">32</x:Double>
        <x:Double x:Key="PictureSize">32</x:Double>
        <CornerRadius x:Key="ControlCornerRadius">16</CornerRadius>
        <Thickness x:Key="ElementMargin">0,0,12,0</Thickness>
    </UserControl.Resources>

    <Grid x:Name="RootGrid"
          Height="{StaticResource ControlHeigth}"
          Background="{ThemeResource ControlFillColorDefaultBrush}"
          BorderBrush="{ThemeResource SolidBackgroundFillColorBaseAltBrush}"
          BorderThickness="1"
          CornerRadius="{StaticResource ControlCornerRadius}"
          Tapped="OnRootGridTapped">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10*" MaxWidth="144" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <muxc:PersonPicture Width="{StaticResource PictureSize}"
                            Height="{StaticResource PictureSize}"
                            Margin="{StaticResource ElementMargin}"
                            DisplayName="{x:Bind DisplayName, Mode=OneWay}"
                            ProfilePicture="{x:Bind Avatar, Mode=OneWay}" />
        <TextBlock x:Name="DisplayNameTextBlock"
                   Grid.Column="1"
                   Margin="{StaticResource ElementMargin}"
                   VerticalAlignment="Center"
                   Text="{x:Bind DisplayName, Mode=OneWay}"
                   TextTrimming="CharacterEllipsis" />
        <TextBlock x:Name="AddressTextBlock"
                   Grid.Column="2"
                   Margin="{StaticResource ElementMargin}"
                   VerticalAlignment="Center"
                   Text="{x:Bind Address, Mode=OneWay}"
                   TextTrimming="CharacterEllipsis" />
        <FontIcon x:Name="ChevronRightIcon"
                  Grid.Column="3"
                  Margin="4"
                  VerticalAlignment="Center"
                  FontFamily="{ThemeResource SymbolThemeFontFamily}"
                  Glyph="&#xE76C;" />

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                    <VisualState.Setters>
                        <Setter Target="RootGrid.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                        <Setter Target="DisplayNameTextBlock.Foreground" Value="{ThemeResource TextFillColorTertiaryBrush}" />
                        <Setter Target="AddressTextBlock.Foreground" Value="{ThemeResource TextFillColorTertiaryBrush}" />
                        <Setter Target="ChevronRightIcon.Foreground" Value="{ThemeResource TextFillColorTertiaryBrush}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>
