<UserControl x:Class="Eppie.App.UI.Controls.PopupHostControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="300"
             d:DesignWidth="400"
             Loaded="OnLoaded"
             mc:Ignorable="d">

    <UserControl.Resources>
        <x:Double x:Key="CloseButtonSize">48</x:Double>
    </UserControl.Resources>

    <Grid Margin="-24">
        <Frame x:Name="PopupHostFrame" />
        <!--
            ToDo: Here we can try to use <Button Style="{StaticResource SubtleButtonStyle}">
            https://github.com/microsoft/microsoft-ui-xaml/blob/main/src/controls/dev/CommonStyles/Button_themeresources.xaml#L297
        -->
        <AppBarButton x:Name="CloseButton"
                      Width="{StaticResource CloseButtonSize}"
                      Height="{StaticResource CloseButtonSize}"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      Click="OnCloseButtonClick"
                      Visibility="{x:Bind IsCloseButtonVisible, Mode=OneWay}">
            <SymbolIcon Symbol="Cancel" />
        </AppBarButton>
    </Grid>
</UserControl>
