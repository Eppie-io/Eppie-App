<local:ContactsPanelPageBase x:Class="Tuvi.App.Shared.Views.ContactsPanelPage"
                             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:local="using:Tuvi.App.Shared.Views"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:sharedControls="using:Tuvi.App.Shared.Controls"
                             xmlns:viewmodels="using:Tuvi.App.ViewModels"
                             mc:Ignorable="d">

    <local:ContactsPanelPageBase.DataContext>
        <viewmodels:BaseViewModel />
    </local:ContactsPanelPageBase.DataContext>

    <Grid Padding="12,0,-32,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Height="40"
                    Margin="4,4,4,8"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
            <SymbolIcon Margin="12,0,12,0" Symbol="Contact" />
            <TextBlock x:Uid="ContactsPanelTitle"
                       Margin="0,0,4,0"
                       VerticalAlignment="Center"
                       Style="{StaticResource PageSubtitleTextStyle}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="4,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ComboBox Grid.Row="0"
                      Margin="4,0,0,0"
                      HorizontalAlignment="Stretch"
                      ItemsSource="{x:Bind ContactsModel.Contacts.SortingVariants, Mode=OneWay}"
                      SelectedIndex="{x:Bind ContactsModel.Contacts.SelectedSortingIndex, Mode=TwoWay}" />

            <AutoSuggestBox x:Uid="/Eppie.App.UI/Controls/ContactFilter"
                            Grid.Row="1"
                            Margin="4,4,0,0"
                            QueryIcon="Find"
                            Text="{x:Bind ContactsModel.Contacts.SearchFilter.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <sharedControls:ContactsListControl Grid.Row="2"
                                                Margin="0,4,0,0"
                                                ContactsModel="{x:Bind ContactsModel, Mode=OneWay}" />
        </Grid>
    </Grid>
</local:ContactsPanelPageBase>
