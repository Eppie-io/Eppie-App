<local:ConnectProtonAddressPageBase x:Class="Eppie.App.Views.ConnectProtonAddressPage"
                                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                    xmlns:behaviors="using:Eppie.App.UI.Behaviors"
                                    xmlns:controls="using:Eppie.App.UI.Controls"
                                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
                                    xmlns:local="using:Eppie.App.Views"
                                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
                                    xmlns:toolkit="using:CommunityToolkit.WinUI"
                                    xmlns:viewmodels="using:Tuvi.App.ViewModels"
                                    Background="Transparent"
                                    Loaded="OnLoaded"
                                    mc:Ignorable="d">

    <local:ConnectProtonAddressPageBase.DataContext>
        <viewmodels:ConnectProtonAddressPageViewModel />
    </local:ConnectProtonAddressPageBase.DataContext>

    <interactivity:Interaction.Behaviors>
        <behaviors:DialogBehavior ApplyCommand="{x:Bind ViewModel.ContinueCommand}"
                                  CancelCommand="{x:Bind ViewModel.ClosedCommand}"
                                  HandleErrorCommand="{x:Bind ViewModel.HandleErrorCommand}" />
    </interactivity:Interaction.Behaviors>

    <local:ConnectProtonAddressPageBase.Resources>
        <x:Double x:Key="PageWidth">428</x:Double>
        <x:Double x:Key="PageMinHeight">500</x:Double>
        <x:Double x:Key="HumanVerifierHeight">592</x:Double>
        <x:Double x:Key="ProgressSize">18</x:Double>
        <x:Double x:Key="IconSize">48</x:Double>
        <x:Double x:Key="IconBorderSize">92</x:Double>
        <CornerRadius x:Key="IconBorderRadius">46</CornerRadius>
        <Thickness x:Key="ControlMargin">0,12,0,12</Thickness>
        <Thickness x:Key="DoneButtonMargin">0,12,0,0</Thickness>
        <Thickness x:Key="PageContentPadding">24,0,24,24</Thickness>

        <viewmodels:ProtonConnectionStep x:Key="CredentialsStepKey">Credentials</viewmodels:ProtonConnectionStep>
        <viewmodels:ProtonConnectionStep x:Key="HumanVerifierStepKey">HumanVerifier</viewmodels:ProtonConnectionStep>
        <viewmodels:ProtonConnectionStep x:Key="TwoFactorCodeStepKey">TwoFactorCode</viewmodels:ProtonConnectionStep>
        <viewmodels:ProtonConnectionStep x:Key="UnlockMailboxStepKey">UnlockMailbox</viewmodels:ProtonConnectionStep>
        <viewmodels:ProtonConnectionStep x:Key="DoneStepKey">Done</viewmodels:ProtonConnectionStep>
    </local:ConnectProtonAddressPageBase.Resources>

    <controls:PopupHostExtensions.TitleContent>
        <Border x:Name="Empty" />
    </controls:PopupHostExtensions.TitleContent>

    <Grid Width="{StaticResource PageWidth}">
        <ScrollViewer x:Name="PageScrollViewer"
                      HorizontalScrollBarVisibility="Disabled"
                      HorizontalScrollMode="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto"
                      ZoomMode="Disabled">
            <Grid MinHeight="{StaticResource PageMinHeight}" Padding="{StaticResource PageContentPadding}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Border Grid.Row="0"
                        Width="{StaticResource IconBorderSize}"
                        Height="{StaticResource IconBorderSize}"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        BorderBrush="{ThemeResource SolidBackgroundFillColorBaseAltBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource IconBorderRadius}">
                    <Image Width="{StaticResource IconSize}"
                           Height="{StaticResource IconSize}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Source="ms-appx:///Assets/Svg/Proton.svg" />
                </Border>

                <Grid Grid.Row="1" Margin="0,28">

                    <StackPanel x:Name="CredentialsStep"
                                Orientation="Vertical"
                                Visibility="Collapsed">
                        <TextBlock x:Uid="ConnectProtonAddressCredentialsStepTitle"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   FontSize="20"
                                   FontWeight="Medium" />
                        <TextBlock x:Uid="ConnectProtonAddressCredentialsStepNote"
                                   Margin="{StaticResource ControlMargin}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="WrapWholeWords" />

                        <controls:ErrorControl HorizontalAlignment="Stretch"
                                               VerticalAlignment="Top"
                                               Property="{x:Bind ViewModel.Email, Mode=OneWay}">
                            <TextBox x:Name="AddressBox"
                                     x:Uid="ConnectProtonAddressEmailAddress"
                                     Margin="{StaticResource ControlMargin}"
                                     IsEnabled="{x:Bind ViewModel.IsProcess, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                                     IsSpellCheckEnabled="False"
                                     Text="{x:Bind ViewModel.Email.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </controls:ErrorControl>

                        <controls:ErrorControl HorizontalAlignment="Stretch"
                                               VerticalAlignment="Top"
                                               Property="{x:Bind ViewModel.Password, Mode=OneWay}">
                            <PasswordBox x:Uid="ConnectProtonAddressPassword"
                                         Margin="{StaticResource ControlMargin}"
                                         IsEnabled="{x:Bind ViewModel.IsProcess, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                                         Password="{x:Bind ViewModel.Password.Value, Mode=TwoWay}" />
                        </controls:ErrorControl>
                        <TextBlock Margin="{StaticResource ControlMargin}" HorizontalAlignment="Left">
                            <Run x:Uid="ConnectProtonAddressNewAccount" />
                            <Hyperlink NavigateUri="{StaticResource NewProtonAccountLink}">
                                <Run x:Uid="ConnectProtonAddressNewAccountLink" />
                            </Hyperlink>
                        </TextBlock>

                    </StackPanel>

                    <StackPanel x:Name="TwoFactorCodeStep"
                                Orientation="Vertical"
                                Visibility="Collapsed">
                        <TextBlock x:Uid="ConnectProtonAddressTwoFactorStepTitle"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   FontSize="20"
                                   FontWeight="Medium" />
                        <TextBlock x:Uid="ConnectProtonAddressTwoFactorStepNote"
                                   Margin="{StaticResource ControlMargin}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="WrapWholeWords" />
                        <TextBox x:Uid="ConnectProtonAddressEmailAddress"
                                 Margin="{StaticResource ControlMargin}"
                                 IsReadOnly="True"
                                 IsSpellCheckEnabled="False"
                                 Text="{x:Bind ViewModel.Email.Value, Mode=OneWay}" />

                        <controls:ErrorControl HorizontalAlignment="Stretch"
                                               VerticalAlignment="Top"
                                               Property="{x:Bind ViewModel.TwoFactorCode, Mode=OneWay}">
                            <TextBox x:Name="TwoFactorCodeBox"
                                     x:Uid="ConnectProtonAddressEmailTwoFactorCodeBox"
                                     Margin="{StaticResource ControlMargin}"
                                     IsEnabled="{x:Bind ViewModel.IsProcess, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                                     IsSpellCheckEnabled="False"
                                     Text="{x:Bind ViewModel.TwoFactorCode.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </controls:ErrorControl>
                    </StackPanel>

                    <StackPanel x:Name="UnlockMailboxStep"
                                Orientation="Vertical"
                                Visibility="Collapsed">
                        <TextBlock x:Uid="ConnectProtonAddressUnlockStepTitle"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   FontSize="20"
                                   FontWeight="Medium" />
                        <TextBlock x:Uid="ConnectProtonAddressUnlockStepNote"
                                   Margin="{StaticResource ControlMargin}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="WrapWholeWords" />
                        <TextBox x:Uid="ConnectProtonAddressEmailAddress"
                                 Margin="{StaticResource ControlMargin}"
                                 IsReadOnly="True"
                                 IsSpellCheckEnabled="False"
                                 Text="{x:Bind ViewModel.Email.Value, Mode=OneWay}" />

                        <controls:ErrorControl HorizontalAlignment="Stretch"
                                               VerticalAlignment="Top"
                                               Property="{x:Bind ViewModel.MailboxPassword, Mode=OneWay}">
                            <PasswordBox x:Name="MailboxPasswordBox"
                                         x:Uid="ConnectProtonAddressUnlockPassword"
                                         Margin="{StaticResource ControlMargin}"
                                         IsEnabled="{x:Bind ViewModel.IsProcess, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                                         Password="{x:Bind ViewModel.MailboxPassword.Value, Mode=TwoWay}" />
                        </controls:ErrorControl>
                    </StackPanel>

                    <StackPanel x:Name="DoneStep"
                                Orientation="Vertical"
                                Visibility="Collapsed">
                        <TextBlock x:Uid="ConnectProtonAddressDoneStepTitle"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   FontSize="20"
                                   FontWeight="Medium" />
                        <TextBlock x:Uid="ConnectProtonAddressDoneStepNote"
                                   Margin="{StaticResource ControlMargin}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Top"
                                   TextTrimming="CharacterEllipsis"
                                   TextWrapping="WrapWholeWords" />
                        <TextBox x:Uid="ConnectProtonAddressEmailAddress"
                                 Margin="{StaticResource ControlMargin}"
                                 IsReadOnly="True"
                                 IsSpellCheckEnabled="False"
                                 Text="{x:Bind ViewModel.Email.Value, Mode=OneWay}" />
                    </StackPanel>

                </Grid>

                <Button x:Name="ContinueButton"
                        Grid.Row="2"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Bottom"
                        Command="{x:Bind ViewModel.ContinueCommand, Mode=OneWay}"
                        IsEnabled="{x:Bind ViewModel.IsProcess, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                        Style="{StaticResource AccentButtonStyle}"
                        Visibility="Collapsed">
                    <Grid>
                        <ProgressRing Width="{StaticResource ProgressSize}"
                                      Height="{StaticResource ProgressSize}"
                                      IsActive="True"
                                      Visibility="{x:Bind ViewModel.IsProcess, Mode=OneWay}" />
                        <TextBlock x:Uid="ConnectProtonAddressContinueButtonContent" Visibility="{x:Bind ViewModel.IsProcess, Converter={StaticResource InverseBoolToVisibilityConverter}, Mode=OneWay}" />
                    </Grid>
                </Button>

                <StackPanel x:Name="DoneButtonPanel"
                            Grid.Row="2"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Bottom"
                            Orientation="Vertical"
                            Visibility="Collapsed">
                    <Button x:Uid="ConnectProtonAddressSettingsButton"
                            HorizontalAlignment="Stretch"
                            Command="{x:Bind ViewModel.OpenSettingsCommand, Mode=OneWay}" />
                    <Button x:Name="DoneButton"
                            x:Uid="ConnectProtonAddressDoneButton"
                            Margin="{StaticResource DoneButtonMargin}"
                            HorizontalAlignment="Stretch"
                            Command="{x:Bind ViewModel.DoneCommand, Mode=OneWay}"
                            Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>

            </Grid>
        </ScrollViewer>

        <Grid x:Name="HumanVerifierStep"
              Height="{StaticResource HumanVerifierHeight}"
              Visibility="Collapsed">
            <!--
                ToDo: Add ScrollViewer.
                Hide vertical scrollbar on WebView2.
                Get body scrollHeight and set WebView2 height to that value.
            -->
            <muxc:WebView2 x:Name="HumanVerifierWebView"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           NavigationCompleted="OnNavigationCompleted"
                           Visibility="Collapsed"
                           WebMessageReceived="OnWebMessageReceived" />
        </Grid>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="Steps" CurrentStateChanged="OnStateChanged">
                <VisualState x:Name="Credentials">
                    <VisualState.StateTriggers>
                        <toolkit:IsEqualStateTrigger Value="{x:Bind ViewModel.Step, Mode=OneWay}" To="{StaticResource CredentialsStepKey}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HumanVerifierStep.Visibility" Value="Collapsed" />
                        <Setter Target="HumanVerifierWebView.Visibility" Value="Collapsed" />
                        <Setter Target="TwoFactorCodeStep.Visibility" Value="Collapsed" />
                        <Setter Target="UnlockMailboxStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneButtonPanel.Visibility" Value="Collapsed" />

                        <Setter Target="PageScrollViewer.Visibility" Value="Visible" />
                        <Setter Target="CredentialsStep.Visibility" Value="Visible" />
                        <Setter Target="ContinueButton.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="HumanVerifier">
                    <VisualState.StateTriggers>
                        <toolkit:IsEqualStateTrigger Value="{x:Bind ViewModel.Step, Mode=OneWay}" To="{StaticResource HumanVerifierStepKey}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="PageScrollViewer.Visibility" Value="Collapsed" />
                        <Setter Target="CredentialsStep.Visibility" Value="Collapsed" />
                        <Setter Target="TwoFactorCodeStep.Visibility" Value="Collapsed" />
                        <Setter Target="UnlockMailboxStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneButtonPanel.Visibility" Value="Collapsed" />
                        <Setter Target="ContinueButton.Visibility" Value="Collapsed" />

                        <Setter Target="HumanVerifierStep.Visibility" Value="Visible" />
                        <Setter Target="HumanVerifierWebView.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="TwoFactorCode">
                    <VisualState.StateTriggers>
                        <toolkit:IsEqualStateTrigger Value="{x:Bind ViewModel.Step, Mode=OneWay}" To="{StaticResource TwoFactorCodeStepKey}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HumanVerifierStep.Visibility" Value="Collapsed" />
                        <Setter Target="HumanVerifierWebView.Visibility" Value="Collapsed" />
                        <Setter Target="CredentialsStep.Visibility" Value="Collapsed" />
                        <Setter Target="UnlockMailboxStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneButtonPanel.Visibility" Value="Collapsed" />

                        <Setter Target="PageScrollViewer.Visibility" Value="Visible" />
                        <Setter Target="TwoFactorCodeStep.Visibility" Value="Visible" />
                        <Setter Target="ContinueButton.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="UnlockMailbox">
                    <VisualState.StateTriggers>
                        <toolkit:IsEqualStateTrigger Value="{x:Bind ViewModel.Step, Mode=OneWay}" To="{StaticResource UnlockMailboxStepKey}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HumanVerifierStep.Visibility" Value="Collapsed" />
                        <Setter Target="HumanVerifierWebView.Visibility" Value="Collapsed" />
                        <Setter Target="CredentialsStep.Visibility" Value="Collapsed" />
                        <Setter Target="TwoFactorCodeStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneStep.Visibility" Value="Collapsed" />
                        <Setter Target="DoneButtonPanel.Visibility" Value="Collapsed" />

                        <Setter Target="PageScrollViewer.Visibility" Value="Visible" />
                        <Setter Target="UnlockMailboxStep.Visibility" Value="Visible" />
                        <Setter Target="ContinueButton.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Done">
                    <VisualState.StateTriggers>
                        <toolkit:IsEqualStateTrigger Value="{x:Bind ViewModel.Step, Mode=OneWay}" To="{StaticResource DoneStepKey}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HumanVerifierStep.Visibility" Value="Collapsed" />
                        <Setter Target="HumanVerifierWebView.Visibility" Value="Collapsed" />
                        <Setter Target="CredentialsStep.Visibility" Value="Collapsed" />
                        <Setter Target="TwoFactorCodeStep.Visibility" Value="Collapsed" />
                        <Setter Target="UnlockMailboxStep.Visibility" Value="Collapsed" />
                        <Setter Target="ContinueButton.Visibility" Value="Collapsed" />

                        <Setter Target="PageScrollViewer.Visibility" Value="Visible" />
                        <Setter Target="DoneStep.Visibility" Value="Visible" />
                        <Setter Target="DoneButtonPanel.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>

</local:ConnectProtonAddressPageBase>
