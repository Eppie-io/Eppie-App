<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        CRITICAL: These dummy instances force the WinUI XAML compiler to generate metadata.
        Without a specific reference in XAML, the x:Uid system cannot resolve
        custom attached properties, resulting in a XamlParseException at runtime.
    -->
    <ToolTipProvider xmlns="using:Eppie.App.UI.Controls" x:Key="DummyToolTipProvider" />
    <TooltipBehavior xmlns="using:Eppie.App.UI.Behaviors" x:Key="DummyTooltipBehavior" />

</ResourceDictionary>
